import React from 'react';
import {CircularProgress, Grid} from "@mui/material";

const Loading = () => {
    return (
        <Grid>
            <CircularProgress/>
        </Grid>
    );
};

export default Loading;
